export const metadata = {
  title: "My Profile | InternsNow",
  description:
    "Manage your student profile, skills, and preferences on InternsNow.",
};

export default function StudentProfileLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
